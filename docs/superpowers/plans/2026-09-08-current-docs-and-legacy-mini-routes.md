# 当前文档与小程序旧入口修复

目标：让入口文档反映 Go 主链路，避免旧小程序入口请求已移除的 Rust 接口。

设计：按用户要求直接删除无 Go 后端支持的旧约队详情、专用组件和 activity/challenge API，不保留兼容页或转跳。旧活动编辑、代报名、签到/互评分支一起清理，当前 Go Match/球队申请/支付流程保留。历史约队和活动通知仍显示内容，不再提供失效详情链接。

约束：不修改后端 API 或数据库；不恢复 Rust 项目；不改管理端既有配置差异；不提交或发布。

- [x] 增加比赛详情非法/旧 ID 不发请求的回归测试，先确认失败。
- [x] 删除旧约队页面、旧活动 API 与不可达 UI 分支；清理专用代码和测试。
- [x] 核对通知跳转，修复球队详情路径，补充回归测试。
- [x] 同步根 README/AGENTS、Go README/AGENTS、管理端 README/AGENTS、小程序 README/架构说明。
- [x] 运行相关 Bun 测试、小程序类型检查、离线小程序构建（MINI_REVIEW_SKIP=1），验证 H5 旧路由与页面。
- [x] 核对 diff 与既有改动，更新项目能力与变更记忆。

验证重点：旧约队详情不再注册；生产页面不引用 activity/challenge API；旧/非法比赛 ID 不请求 /activity；Go Match 请求失败不能回落旧接口；新比赛创建保持原流程；通知仍可进入当前详情。


验证结果：
- 小程序 `bun run type-check` 通过。
- 相关测试 105 项通过；独立运行 Go 报名 hook 行为测试 7 项通过（共 112 项）。
- `MINI_REVIEW_SKIP=1 bun run build:mp-weixin` 通过，组件注册检查通过；编译产物中旧约队详情路由已移除。
- Playwright 使用本地 H5 与拦截的测试接口验证：无效比赛 ID、无链接通知标为已读且不跳转、Go 比赛详情/编辑弹窗/创建页；没有旧 activity/challenges 请求或页面异常。
- 独立只读审查未发现误删 Go 活跃行为；`git diff --check` 通过。
- 未运行 Go 或管理端构建/测试：这两端只改文档。未做微信真机验证、提交或发布。
