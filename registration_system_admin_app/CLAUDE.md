# registration_system_admin_app 对话助手说明

你正在维护一个 Flutter 移动端管理 App。

## 当前定位

- 这是给管理人员使用的移动端，不是给普通队员的入口。
- 第一个版本优先围绕 `创建比赛` 和 `创建球队` 这类管理任务。

## 约定

- 页面优先保持为编排层。
- 复用的表单样式、校验和卡片组件尽量抽到独立文件。
- 与后端交互时，以实际 API 和 DTO 为准，不猜字段。
